playbook:
  metadata:
    name: Web Application Reconnaissance (High-Frequency Optimized v2)
    id: web_app_recon_v1_optimized_v2
    version: '2.0'
    base_playbook_id: web_app_recon_v1
    category: Reconnaissance
    optimization_focus: High-frequency vulnerability patterns by target archetype
    scope_lock_required: true
  optimization_baseline:
    baseline_execution_minutes: 45
    optimized_execution_minutes: 15
    baseline_success_rate: 0.8
    optimized_success_rate: 0.92
    expected_time_reduction_percent: 66.7
    expected_success_uplift_percent: 15.0
  pre_execution_target_classification:
    required_playbooks:
    - target_fingerprinter_v1
    - archetype_classifier_v1
    - vulnerability_predictor_v1
    required_outputs:
    - target_archetype
    - confidence_score
    - recommended_top_playbooks
    scope_validation_first: true
  target_aware_routing:
    startup:
      exploitation_patterns:
      - verbose_error_leak
      - open_metadata_disclosure
      - backup_artifact_exposure
      timeout_seconds: 180
      expected_success_rate: 0.95
      execution_mode: high_frequency_only
    enterprise:
      exploitation_patterns:
      - debug_telemetry_data_leak
      - object_storage_policy_disclosure
      - api_response_overexposure
      timeout_seconds: 300
      expected_success_rate: 0.95
      execution_mode: high_frequency_only
    government:
      exploitation_patterns:
      - classified_field_overexposure_check
      - metadata_boundary_validation
      timeout_seconds: 480
      expected_success_rate: 0.78
      execution_mode: high_frequency_only
  execution_logic:
    selection_rule: Use target archetype route; execute only listed high-frequency
      patterns.
    early_exit: Stop on first validated critical/high finding or after pattern list
      exhausted.
    fallback: If archetype unknown, run startup route with enterprise timeout.
  optimization_rationale: Original playbook 'web_app_recon_v1' executed broad pattern
    sets. v2 reduces low-probability pattern execution and targets high-frequency
    info_disclosure paths for startup/enterprise/government archetypes using Prompt
    5 frequency intelligence.
