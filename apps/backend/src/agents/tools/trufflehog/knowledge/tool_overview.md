# Trufflehog Tool Overview

Git history secret scanner for verified credential discovery. Scans git repositories including deleted commits. Finds API keys, tokens, passwords, and private keys. Verified-only mode filters out false positives. Critical severity findings. Both tools required for maximum coverage.
