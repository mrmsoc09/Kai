Prefer auxiliary scanner modules before exploit modules.
Pin module options explicitly (`RHOSTS`, `RPORT`, `SSL`, payload options).
Capture console output and session metadata as evidence, and cross-check findings with independent tools.
