# Gitleaks Tool Overview

Regex-based git secret detector. Scans git history using configurable rulesets. Identifies API keys, credentials, tokens, private keys. Different ruleset from Trufflehog. High-accuracy regex patterns. Both tools required for complete coverage.
