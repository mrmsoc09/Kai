# Onionsearch Tool Overview

Dark web search aggregator querying multiple .onion indices without requiring local Tor crawling. Query-based approach finds indexed data dumps, credential listings, and org mentions. Faster than crawling. Complements Torbot with different index coverage and search methodology.
