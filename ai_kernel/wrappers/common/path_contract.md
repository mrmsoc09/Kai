Paths:
- Artifacts: `artifacts/<run_id>/<tool_id>/`
- Runtime memory: `runtime/memory/{shared,sessions,artifacts,indexes}`
- Logs/metrics/traces: `runtime/{logs,metrics,traces}`
- Reports: `runtime/reports`
