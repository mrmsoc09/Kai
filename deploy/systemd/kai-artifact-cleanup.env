# Host directory for all tool artifacts.
K1_ARTIFACTS_HOST_ROOT=/var/kai-artifacts

# Delete files older than this many days before quota checks.
K1_ARTIFACT_RETENTION_DAYS=14

# 100 GiB limit per tool output directory.
K1_ARTIFACT_MAX_BYTES_PER_TOOL=107374182400
