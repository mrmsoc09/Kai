# fullhunt Tool Overview

FullHunt provides attack-surface intelligence for domain subdomains, cloud hints, and observed ports that can guide downstream reconnaissance priorities.

This agent is wired for passive collection, deterministic parsing, and governance-safe evidence generation. It only emits structured findings and hands off to downstream agents for verification rather than active exploitation.
