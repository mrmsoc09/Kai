
## Architecture

The suite uses an async/await architecture for high concurrency, with semaphore-based connection pooling and queue-based payload distribution. The AI feedback loop analyzes successful payloads to guide mutation strategies, improving efficiency over time.
