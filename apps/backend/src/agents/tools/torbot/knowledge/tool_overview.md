# Torbot Tool Overview

Tor-based dark web spider for discovering organizational data exposure. Implements read-only crawl with configurable depth (maximum 2 levels to prevent runaway scans). Identifies target organization mentions, credential patterns, and data breach indicators across .onion domains. Requires Tor service on port 9050. Passive reconnaissance only.
