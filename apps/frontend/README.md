K1 Frontend (Vite + React)

- Dev: npm run dev
- Build: npm run build
- Preview: npm run preview

Environment:
- VITE_API_BASE (default http://localhost:8080)

Routes:
- /dashboard — keys + health
- /operations — HiL flow driver (create → evidence → request → approve → submit)
- /mcp-registry — provider catalog/selection UI
