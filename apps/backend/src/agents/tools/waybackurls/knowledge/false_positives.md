# waybackurls — False Positives

## Historical URLs May 404 Currently
Same as gau — historical URLs may not be live. Always verify live status with httpx before testing.

## Outdated Parameters
Parameter names from old URLs may not exist on current version of the app. Use as starting point for fuzzing, not guaranteed valid targets.
