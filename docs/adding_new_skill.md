# Adding a Skill

- Author shared content in `ai-kernel/skills/` using the template.
- Run `python scripts/export_skill_views.py` to sync adapters.
- Register in `config/registry/skill_registry.yaml` if routable.
