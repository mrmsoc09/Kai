# Runtime Operations

- Start orchestrators or adapters after running validation/smoke scripts.
- Logs: `runtime/logs`, Metrics: `runtime/metrics`, Traces: `runtime/traces`.
- Reports stored in `runtime/reports`.
- Memory separated under `runtime/memory/*`; avoid writing vendor data outside this tree.
