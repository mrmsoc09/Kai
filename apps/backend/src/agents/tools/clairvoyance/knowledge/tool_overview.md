# Clairvoyance Tool Overview

GraphQL schema recovery via wordlist when introspection disabled. Finds field names by fuzzing common names and sensitive patterns (password, token, secret, admin, internal). High-value discoveries for schema-hidden APIs. Intrusive classification. Runs only after GraphQL-Cop confirms introspection is disabled.
