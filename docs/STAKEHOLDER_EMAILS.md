# Stakeholder Email Templates

Use /mailer/templates to list available templates and /mailer/render to render a subject/body for a given stakeholder with context fields: {title, severity, scope, summary, mitigation}.
Sending is blocked by policy until HiL approval.
