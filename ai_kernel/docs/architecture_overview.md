# Kai AI Kernel Architecture

This kernel provides shared governance, schemas, skills, and wrappers consumed by platform-specific adapters (Gemini CLI, Claude Code, Codex). Shared logic lives in `ai-kernel/`; adapters mirror the content without diverging safety posture.
