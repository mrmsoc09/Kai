# GraphQL-Cop Tool Overview

GraphQL security test suite. Tests introspection, batching, field suggestion, depth limits, alias abuse, circular queries. Runs after Katana confirms GraphQL endpoint. High-value findings from introspection enabled. Intrusive classification.
