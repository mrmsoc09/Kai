# Runtime Contracts

- Artifacts: `artifacts/<run_id>/<tool_id>/` with SHA256 hashes.
- Memory: `runtime/memory/{shared,sessions,artifacts,indexes}` with integrity metadata.
- Logs/metrics/traces under runtime/.
- Schemas for findings, reports, tool results, and artifacts live in `ai-kernel/governance/schemas`.
